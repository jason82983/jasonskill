interface:
  display_name: "HZP Amazon 6-0-2｜AI精准关键词识别"
  short_description: "完整判断601对标关键词精准度并输出603/606正式资产"
  brand_color: "#2563EB"
  default_prompt: "Use $hzp-amz-6-0-2-ai-precision-keyword-identification for the supplied ProductCode and complete the entire business task in this invocation. Read the three formal business inputs, build one validated runtime Product Profile, apply the Precision Brain V2 Hard Conflict Gate and Current Product Answer Role before grading every canonical unique keyword, continue internal batches until PendingJudgmentCount is zero, and publish the four complete sequence-numbered output classes only after full reconciliation. Never ask the caller to invoke the Skill again for another batch. The current Agent is the only precision judge; deterministic code must never judge precision."

policy:
  allow_implicit_invocation: true
