interface:
  display_name: "6-0-3｜精准泛词提取"
  short_description: "从602最新C资产建立稳定Current Product Search Intent Map"
  default_prompt: "Use $hzp-amz-6-0-3-precision-broad-extraction. Read only the newest filename-timestamped 6-0-2 C asset from 06_SKILL分析报告/6-0-2_AI精准关键词识别/data/; do not require RunPackage, Manifest, sidecar, or mtime. Validate one canonical keyword per row and stop on INPUT_NOT_UNIQUE. Build semantic units then globally reconcile Search Intents, assign exactly one Primary Intent and at most one semantic parent per keyword, preserve stable IntentId/Code, calculate all market metrics and reconciliation in code, and emit the mapping B CSV before deriving summary A. Never rejudge precision, query ERP, build benchmark-specific trees, or execute ads. Preserve trace fields and use the 6-0-3 data directory with timestamped outputs."
